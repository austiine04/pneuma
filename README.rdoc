== README

To get started, you need to install 2.2 on your computer

* clone this repository 

* cd into the source code directory and run;
    * bundle install
    * rake bower:install
    * rake db:migrate, to set up the database
    * foreman start to run server, you will need to get the aws keys from a dev on the project.
